name: FLUTTER_APPLICATION_3
description: A new Flutter project

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.6
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  fl_chart: ^0.41.0
  cloud_firestore: ^4.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/  # Substitua pelo caminho correto para seus ativos
environment:
  sdk: '>=3.1.0 <4.0.0'
